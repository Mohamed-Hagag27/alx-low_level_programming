#include "main.h"
/**
 * _abs - compute the absolute value of a number
 * Return: absolute value.
 * @n: number to print its aboslute value.
*/
int _abs(int n)
{
if (n < 0)
return (n * -1);
else
return (n);
}
