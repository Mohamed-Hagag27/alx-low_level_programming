#include"main.h"
#include <stdio.h>
/**
 * main - main function
 * Return: always 0.
 * @argc: number of arguments.
 * @argv: string array containing the arguments.
 */
int main(int argc, char *argv[])
{

printf("%s\n", argv[argc * 0]);

return (0);
}
