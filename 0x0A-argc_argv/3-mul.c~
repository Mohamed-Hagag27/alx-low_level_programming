#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * main - main function
 * @argc: number of arguments
 * @argv: list of arguments
 * Return: 1 or zero
 */

int main(int argc, char *argv[]) {
int num1, num2;
if (argc != 3){
printf("Error");
return (1);
}

num1 = atoi(argv[1]);

num2 = atoi(argv[2]);

printf("%d\n", num1 * num2);
return (0);
}
