#include"main.h"
/**
 * _isupper - checks if a char is upper case or lower case.
 * @c: the variable to check.
 *
 * Return: differs.
 */
int _isupper(int c)
{
if (c >= 65 && c <= 90 )
return (1);

return (0);
}
