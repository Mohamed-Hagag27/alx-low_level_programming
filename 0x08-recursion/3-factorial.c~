#include"main.h"
/**
 * factorial - returns factorial of particular number/
 * @n:a number to get its factorial.
 * Return: difers
 */
int factorial(int n)
{
if (n < 0)
return (-1);
if (n == 0)
return (1);
return (n * factorial(n-1));
}
