#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase
 *
 * Return: always 0
 */
int main() {
for (char alphabet = 'a'; alphabet < 123 ; alphabet++)
putchar(alphabet);

putchar(' ');
return (0);
}
